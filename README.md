
## ✍️ Enums and keyof
### ⭐ TypeScript-এ Enums এর ব্যবহার (Numeric এবং String Enum উদাহরণসহ)

TypeScript-এ enum ব্যবহার করা হয় নির্দিষ্ট কিছু মানকে একটি গ্রুপে সাজিয়ে রাখার জন্য। যখন কোনো মান কয়েকটি নির্দিষ্ট অপশনের মধ্যে সীমাবদ্ধ থাকে, তখন enum ব্যবহার করলে ভালো হয়।


Numeric enum-এ মানগুলো স্বয়ংক্রিয়ভাবে 0 থেকে শুরু করে।

enum Status {
    Pending,
    Approved,
    Rejected
}

let currentStatus: Status = Status.Approved;


এখানে:

Pending = 0

Approved = 1

Rejected = 2

String Enum

String enum-এ প্রত্যেকটি মানকে স্ট্রিং হিসেবে নির্ধারণ করা হয়।

enum Role {
    Admin = "ADMIN",
    User = "USER",
    Viewer = "VIEWER"
}

let userRole: Role = Role.User;


String enum সাধারণত এমন ক্ষেত্রে ব্যবহার করা হয় যেখানে readable output প্রয়োজন বা নির্দিষ্ট স্ট্রিং মান API বা UI-তে ব্যবহৃত হয়।

### ⭐ TypeScript-এ keyof এর ব্যবহার (উদাহরণসহ)

TypeScript-এ keyof ব্যবহার করা হয় কোনো object টাইপের property গুলোর নামকে union টাইপ হিসেবে পাওয়ার জন্য। এটি কোডকে আরও type-safe এবং flexible করে।

ধরুন একটি Person টাইপ আছে:

type Person = {
    name: string;
    age: number;
    country: string;
};


এখন:

//keyof Person    // "name" | "age" | "country"

//Example:
const getValue = (obj: Person, key: keyof Person) => {
    return obj[key];
};

const person: Person = { name: "Rafi", age: 25, country: "Bangladesh" };

getValue(person, "name");     
getValue(person, "country");
// ভুল: getValue(person, "address");  // TypeScript error


keyof নিশ্চিত করে যে ভুল property নাম দিলে TypeScript সঙ্গে সঙ্গে ত্রুটি দেখাবে।

## 🧾 Code File

👉 সব সমস্যার সমাধান solution.ts ফাইলে যুক্ত করা হয়েছে।
👉 ফাংশনগুলোর নাম এবং আউটপুট অ্যাসাইনমেন্টের নির্দেশনা অনুযায়ী রাখা হয়েছে।
👉 কোনো console.log() ব্যবহার করা হয়নি।
